# Indoor Voice


def main():
    """
    Prompts the user for input and then outputs that same input in lowercase.
    Punctuation and whitespace should be outputted unchanged. You’re welcome, but
    not required, to prompt the user explicitly, as by passing a str of your own as
    an argument to input.
    """

    print("Output:", input("Enter an upper-case string: ").lower())


main()
